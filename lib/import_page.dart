import 'package:flutter/material.dart';

class ImportPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Import Data'),
        backgroundColor: Color(0xFF4CAF50),
      ),
      body: Center(
        child: Text('Halaman Import Data'),
      ),
    );
  }
}
